# Used only during server-side builds or when deploying on a Next.js host.
# Keep this value private; never use a NEXT_PUBLIC_ prefix for it.
GITHUB_TOKEN=
NEXT_PUBLIC_SITE_URL=https://berserk-23115.github.io
